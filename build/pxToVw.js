module.exports = {
  viewportWidth: false, // 对应设计图的宽度，用于计算 vw。默认 750，指定 0 或 false 时禁用
  rootValue: false, // 根字体大小，用于计算 rem。默认 75，指定 0 或 false 时禁用
  unitPrecision: 2, // 计算结果的精度，默认 5
  minPixelValue: 1// 小于等于该值的 px 单位不作处理，默认 1
};
